# results/analysis

This directory holds derived artifacts produced **after** the experiment was
finished, during the repository audit. The original experimental results live in
the root of `results/` (`metrics_ml.csv`, `metrics_dl.csv`,
`metrics_crossdataset.csv`, `manifests/`) and were not altered.
