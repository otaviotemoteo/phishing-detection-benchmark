# =============================================================================
# Phishing Detection Benchmark - Pinned Dependencies
# =============================================================================
# Python: 3.11.x (3.12+ has compatibility issues with some ML libs)
#
# CRITICAL: All versions are pinned with == for reproducibility.
# Do not upgrade without documenting the change in docs/DECISIONS.md.
#
# Install with: pip install -r requirements.txt
# Then run:     python scripts/verify_environment.py
# =============================================================================

# Core scientific stack
numpy==1.26.4
pandas==2.2.3
# scikit-learn's numerical backend; pinned so it cannot drift
scipy==1.14.1

# Classical ML
scikit-learn==1.5.2
imbalanced-learn==0.12.4

# Gradient boosting
xgboost==2.1.1
catboost==1.2.7

# Deep Learning (PyTorch with CUDA 12.1 on the reference platform)
# On macOS arm64 the same pin resolves to the MPS/CPU build; on CPU-only or other
# CUDA versions, see https://pytorch.org/get-started/locally/
torch==2.4.1

# Experiment tracking
mlflow==2.16.2

# Visualization
matplotlib==3.9.2
seaborn==0.13.2

# System monitoring (computational cost)
psutil==6.0.0
# NVIDIA GPU memory; degrades to None where there is no NVML
py3nvml==0.2.7

# Utilities
joblib==1.4.2
# mlflow's config backend, and CITATION.cff validation
pyyaml==6.0.2

# Notebooks (scripts/run_all.sh executes them through nbconvert)
jupyter==1.1.1
ipykernel==6.29.5
ipywidgets==8.1.5

# Tests
pytest==8.3.3

# Code quality
ruff==0.6.9
black==24.10.0
